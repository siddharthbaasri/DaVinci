export type Course = {
    id: number;
    title: string;
    description: string;
    subject: string;
};

export type User = {
    id: number;
    name: string;
    email: string;
    image: string;
}